{
  "name": "disk-change-monitor",
  "version": "1.0.0",
  "description": "Help find where disk space is going.  Displays changed files and folders since the last time it was run",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node index.js"
  },
  "keywords": [
    "disk",
    "space",
    "monitor"
  ],
  "author": "Jim Holmes",
  "license": "MIT",
  "devDependencies": {
    "electron": "2.0.0"
  },
  "dependencies": {
    "electron": "^2.0.0",
    "electron-pug": "^1.5.1"
  }
}
